#include <stdio.h>
#include <stdlib.h>
#include <time.h>
#include "fichier.h"
/*int main()
{
    int a,b, result = 0;
    char op;


    do
    {
        printf("Saisir deux valeurs et une operation parmis les suivantes : +, -, /, * ou ! pour quitter\n");
        scanf("%d %c %d",&a,&op,&b);
    }while( op != '+' && op != '-'&& op!='/' && op!= '!');
    {
        switch(op)
        {
            case '+':
                addition(&a,&b,&result);
                printf("%d",result);
                break;
            case '-':
                soustraction(&a,&b,&result);
                printf("%d",result);
                break;
            case '/':
                division(&a,&b,&result);
                printf("%d",result);
                break;
            case'':
                multiplication(&a,&b,&result);
                printf("%d",result);
                break;
            case '!' :
                break;
        }
    }

} */







void  fonction_main(){
    int x;
    int y=0 ;
    int *px;
    int *py;
    printf("entrer une valeur");
    scanf("%d",x);
    printf("l'adresse de x est: %p. l'adresse de y est: %p", x, y);
}

        int main(){

}


























